interface Theme {
  primaryColor: string
  secondaryColor: string
  backgroundColor: string
}

const theme: Theme = {
  primaryColor: '#0718BE',
  secondaryColor: '#FC5E5D',
  backgroundColor: '#7C72EB',
}

export default theme
